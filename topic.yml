apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: rapid
  namespace: arbeidsgiver-inkludering
  labels:
    team: arbeidsgiver-inkludering
  annotations:
    kafka.nais.io/removeDataWhenResourceIsDeleted: "true"
spec:
  pool: nav-dev
  config: # optional; all fields are optional too
    cleanupPolicy: delete
    minimumInSyncReplicas: 1
    partitions: 1
    replication: 3
    retentionBytes: -1 # -1 means unlimited
    retentionHours: 336 # 336 = 14 dager
  acl:
    - team: arbeidsgiver
      application: toi-arbeidsplassen-cv-reader
      access: readwrite # read, write, readwrite
    - team: arbeidsgiver-inkludering
      application: arbeidsgiver-inkludering-kafkamanager
      access: readwrite # read, write, readwrite
