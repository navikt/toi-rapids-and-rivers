apiVersion: 'nais.io/v1'
kind: 'Alert'
metadata:
  name: toi-kandidatfeed
  namespace: toi
  labels:
    team: toi
spec:
  receivers:
    slack:
      channel: 'inkludering-alerts-prod'
  alerts:
    - alert: toi-kandidatfeed
      severity: danger
      expr: sum(up{app="toi-kandidatfeed", job="kubernetes-pods"}) == 0
      for: 1s
      description: Appen er nede (ingen Kubernetes-pods er oppe)
      action: "https://logs.adeo.no/goto/bdc97edca180aae5764592509f2bb548"

    - alert: toi-kandidatfeed
      expr: sum(increase(logd_messages_total{log_app="toi-kandidatfeed",log_level="Error"}[10m])) > 0
      for: 1s
      description: Appen har logget en error
      action: "https://logs.adeo.no/goto/bdc97edca180aae5764592509f2bb548"

    - alert: toi-kandidatfeed
      severity: warning
      expr: (100 * sum by (log_app, log_namespace) (rate(logd_messages_total{log_app="toi-kandidatfeed",log_level=~"Warning"}[5m])) / sum by (log_app, log_namespace) (rate(logd_messages_total{log_app="toi-kandidatfeed"}[5m]))) > 20
      for: 5m
      description: Økning i warnings
      action: "https://logs.adeo.no/goto/bdc97edca180aae5764592509f2bb548"
